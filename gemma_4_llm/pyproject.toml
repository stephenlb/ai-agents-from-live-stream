[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gemma_4_llm"
version = "1.0.0"
description = "gemma_4_llm agent"
requires-python = ">=3.12"
dependencies = [
    "blocks-network",
    "ollama",
]

[tool.setuptools]
py-modules = ["handler"]
